"""
FALCON OpenEnv Client
For local debugging and integration.
"""

from server.models import Action, Observation, Reward, StepResponse, EpisodeState

# Standard OpenEnv Pythonic wrapper mappings can go here.
# For OpenEnv evaluation the primary evaluation vector is the Docker HTTP API.
